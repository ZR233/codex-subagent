---
name: codex-subagent
description: 在代码库中按需派发受限子代理执行大范围探索或独立实现；智谱优先，高峰或额度不足时使用 DeepSeek，主代理负责验收和原会话返修。
metadata:
  short-description: 智谱优先的子代理派发与证据验收
---

# Codex 子代理

本技能供主代理派发独立任务。探索和编码由受限执行者完成；主代理负责目标、架构、任务边界、测试、集成与发布。不要用调用成功替代功能验收。

## 执行者身份

若启动器注入的子代理执行者标记已设置，或任务明确说「你是子代理执行者」，直接完成分配的任务，不再运行本启动器或任何代理命令，也不派发子代理。探索任务只读；实现任务只修改授权范围，可编写测试代码，仅对改动文件做必要的局部格式化。禁止执行测试、编译、静态检查、运行验证或环境探测；不确定时交回主代理。不要撤销协作者的改动。交接说明改动、风险和未执行的验证。

否则你是主代理。依据任务范围决定是否派发：大文件、跨目录检索、独立的探索或实现适合子代理；已知位置的少量代码、即将修改的关键接口与奠基文档由主代理亲自阅读。不要为了凑并行数启动无用会话。

## 派发任务

1. 先读架构与接口依据，再按相互独立的文件、目录或功能分配所有权。共享文件指定唯一负责者；有依赖时先确定接口。并行编码应使用独立工作树或隔离副本，并保留已有未提交改动。
2. 为每项任务准备独立的 UTF-8 提示文件，写明目标、工作目录、可改范围、接口和验收入口。不要把完整实现补丁交给子代理照抄。
3. 从本技能目录运行 `scripts/run_subagent.py`。示例：

   ```powershell
   python "$HOME\.codex\skills\codex-subagent\scripts\run_subagent.py" --mode explore --cwd "C:\path\to\repo" --prompt-file "C:\path\to\task.txt" --output-dir "C:\path\to\evidence"
   ```

   ```bash
   python3 "${CODEX_HOME:-$HOME/.codex}/skills/codex-subagent/scripts/run_subagent.py" --mode implement --cwd /path/to/repo --prompt-file /path/to/task.txt --output-dir /path/to/evidence
   ```

   `--mode explore` 使用只读沙箱；`--mode implement` 使用工作区可写沙箱。`--output-dir` 每轮必须全新或为空，`--timeout` 默认为每次尝试 900 秒。计划模式只派发探索任务。`--provider zai|deepseek` 显式指定服务，`--peak-window auto|on|off` 分别表示按时钟判断、强制高峰及关闭高峰判断。

## 服务选择

本机须分别配置 Codex 的 `zai` 与 `deepseek` 配置文件和凭据；技能不代管模型或密钥。新任务默认选择智谱；每日 14:00–18:00（北京时间）直接选用 DeepSeek。可显式指定服务，或强制、禁用高峰判断。智谱出现明确额度耗尽、限流或服务过载的实际错误时，仅在尚未开始任何工具调用且并非续跑的情况下自动改用 DeepSeek 一次。普通失败、超时、取消、内部错误和已经可能产生副作用的运行都不切换。不可把自动回退当作无限重试机制。

续跑沿用上轮确切会话和服务，不跨服务迁移上下文。智谱会话在续跑时若已进入高峰，启动器在创建证据或调用模型前返回错误；`--peak-window off` 不能绕过此限制。智谱续跑若额度耗尽，失败状态中的 `next_action=start_new_session` 与 `recommended_provider=deepseek` 会要求主代理先核对已有改动，再用 `--provider deepseek` 将尚未完成的工作交给新会话。新任务不得传入旧的 `--resume-from`，也不要无证据重复执行已完成的实现。

## 验收与返修

- 并发最多 30 个，没有默认数量。逐个跟踪任务标识、所有权、依赖、工作位置、会话标识、证据目录与验收状态。一个任务完成就验收，不必等待不相关的任务。
- 查看顶层 `status.json` 与 `final.txt`，必要时定位 `attempt-1/`、`attempt-2/` 中的 `stderr.log`、`events.jsonl` 和受影响源码。自动回退时两次尝试的证据均须保留。`succeeded` 只说明命令行正常结束，不能表示行为正确。
- 主代理亲自执行静态检查、编译、测试及真实入口验证，覆盖成功、边界与失败路径。验收通过后仅集成已授权且已验收的改动，核对没有覆盖其他工作。
- 返修将问题、复现、期望与实际结果写入新的提示文件，以 `--resume-from <上一轮证据目录>` 续接原任务，输出到新的空目录。模式和工作目录保持一致。旧会话缺失或损坏时报告问题，不悄悄新建会话。

禁止再委派采用模型能力关闭、行为规则和工具事件审计共同约束，不是能防御恶意绕过的预执行安全隔离。提交、推送与发布等外部操作不能从子代理派发中自动获得授权。
